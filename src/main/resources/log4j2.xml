<?xml version="1.0" encoding="UTF-8"?>
<Configuration status="WARN">

    <Appenders>

        <Console name="Console" target="SYSTEM_OUT">
            <PatternLayout pattern="%d{HH:mm:ss} [%-5level] %msg%n"/>
        </Console>

        <RollingFile name="GeneratorLog"
                     fileName="logs/maze-generator.log"
                     filePattern="logs/maze-generator-%d{yyyy-MM-dd}.log">
            <PatternLayout pattern="%d{yyyy-MM-dd HH:mm:ss} [%-5level] %msg%n"/>
            <Policies>
                <TimeBasedTriggeringPolicy/>
            </Policies>
        </RollingFile>

        <RollingFile name="SolverLog"
                     fileName="logs/maze-solver.log"
                     filePattern="logs/maze-solver-%d{yyyy-MM-dd}.log">
            <PatternLayout pattern="%d{yyyy-MM-dd HH:mm:ss} [%-5level] %msg%n"/>
            <Policies>
                <TimeBasedTriggeringPolicy/>
            </Policies>
        </RollingFile>

    </Appenders>

    <Loggers>

        <Logger name="MazeGenerator" level="info" additivity="false">
            <AppenderRef ref="GeneratorLog"/>
            <AppenderRef ref="Console"/>
        </Logger>

        <Logger name="MazeSolver" level="info" additivity="false">
            <AppenderRef ref="SolverLog"/>
            <AppenderRef ref="Console"/>
        </Logger>

        <Root level="warn">
            <AppenderRef ref="Console"/>
        </Root>

    </Loggers>

</Configuration>
